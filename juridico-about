<!-- À PROPOS - SpeedUp / Juridico Answer - section -->
<section class="about-me">
  <div class="container">
    <div class="profile">
      <div class="avatar">
        <!-- Remplace src par ta photo -->
        <img src="https://via.placeholder.com/320x320.png?text=Votre+photo" alt="Photo" />
      </div>
      <div class="badge">Juridico Answer</div>
    </div>

    <div class="content">
      <h2 class="title">Qui je suis &amp; ce que je fais</h2>

      <p class="lead">
        Juridico Answer accompagne les auto-entrepreneurs, micro-entreprises, petites structures et particuliers.
        Mon objectif : vous faire gagner du temps, clarifier vos démarches et constituer vos dossiers correctement — en ligne, avec vous ou pour vous (sur mandat).
      </p>

      <ul class="services">
        <li><strong>Accompagnement administratif</strong> — dossiers, formulaires et démarches en ligne.</li>
        <li><strong>Montage de dossiers</strong> — vérification, organisation et envoi conforme.</li>
        <li><strong>Orientation spécialisée</strong> — mise en relation avec un avocat partenaire ou un expert-comptable si besoin.</li>
      </ul>

      <p class="note">
        <strong>Important :</strong> Juridico Answer n’est pas un cabinet d’avocats. Nous ne fournissons pas de consultations juridiques réservées aux avocats ni d’actes judiciaires ou contentieux. Si votre situation l’exige, nous vous orientons vers un avocat ou un spécialiste.
      </p>

      <div class="cta-row">
        <a class="btn primary" href="#contact">Contactez-moi</a>
        <a class="btn ghost" href="#services">Voir les services</a>
      </div>
    </div>
  </div>
</section>

<style>
:root{
  --bg: #ffffff;
  --blue-deep: #0A1A2F;    /* titre / texte fort */
  --cyan: #12C2B9;         /* accent / CTA */
  --muted: #6E7A90;       /* texte secondaire */
  --card: #F5F7FA;        /* card background */
  --radius: 18px;
  --container-width: 1100px;
  --max-mobile-width: 980px;
  --gap: 28px;
  font-family: "Inter", "Segoe UI", Roboto, Arial, sans-serif;
}

.about-me{
  padding: 64px 20px;
  background: var(--bg);
  color: var(--blue-deep);
}

.about-me .container{
  max-width: var(--container-width);
  margin: 0 auto;
  display: grid;
  grid-template-columns: 420px 1fr;
  gap: var(--gap);
  align-items: center;
}

/* left column - avatar */
.profile{
  display:flex;
  flex-direction:column;
  align-items:center;
  gap:18px;
}
.avatar{
  width:320px;
  height:320px;
  border-radius:20px;
  overflow:hidden;
  box-shadow: 0 14px 40px rgba(10,26,47,0.08);
  border: 1px solid rgba(12,31,60,0.04);
}
.avatar img{
  width:100%;
  height:100%;
  object-fit:cover;
  display:block;
  transition: transform .6s ease;
}
.avatar img:hover{ transform: scale(1.03); }

.badge{
  background: linear-gradient(90deg, rgba(18,194,185,0.12), rgba(18,194,185,0.08));
  color:var(--blue-deep);
  padding:10px 18px;
  border-radius:30px;
  font-weight:600;
  letter-spacing:0.2px;
  box-shadow: 0 6px 18px rgba(18,194,185,0.08);
}

/* right column - content */
.content .title{
  font-size:34px;
  margin:0 0 12px 0;
  line-height:1.05;
  color:var(--blue-deep);
}

.content .lead{
  margin:0 0 18px 0;
  color:var(--muted);
  font-size:16px;
  max-width: 720px;
}

.services{
  margin:0 0 18px 0;
  padding:0;
  list-style:none;
  display:grid;
  gap:10px;
}
.services li{
  background: var(--card);
  padding:12px 14px;
  border-radius:12px;
  color:var(--blue-deep);
  font-size:15px;
  box-shadow: 0 8px 20px rgba(10,26,47,0.03);
}
.services li strong{ color:var(--blue-deep); }

.note{
  margin:14px 0 22px 0;
  color:var(--muted);
  font-size:14px;
  background: rgba(10,26,47,0.02);
  padding:12px 14px;
  border-radius:10px;
}

/* CTA */
.cta-row{
  display:flex;
  gap:12px;
  align-items:center;
  margin-top:6px;
}
.btn{
  display:inline-block;
  padding:12px 20px;
  border-radius:12px;
  text-decoration:none;
  font-weight:700;
  font-size:15px;
  transition: transform .18s ease, box-shadow .18s ease;
}
.btn.primary{
  background: linear-gradient(90deg, var(--cyan), #09b39b);
  color:#fff;
  box-shadow: 0 12px 30px rgba(18,194,185,0.14);
}
.btn.primary:hover{ transform: translateY(-4px); box-shadow: 0 18px 45px rgba(18,194,185,0.18); }
.btn.ghost{
  background: transparent;
  color:var(--blue-deep);
  border: 1px solid rgba(10,26,47,0.06);
}
.btn.ghost:hover{ transform: translateY(-3px); background: rgba(10,26,47,0.02); }

/* responsive */
@media (max-width: 980px){
  .about-me .container{ grid-template-columns: 1fr; }
  .avatar{ width:240px; height:240px; }
  .content{ padding-top:6px; }
  .content .title{ font-size:28px; }
  .profile{ order:0; }
  .content{ order:1; }
}
</style>
